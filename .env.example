# Copy to .env and fill in. .env is gitignored.
#
# Create these keys with TRADE permission only.
# Never enable withdrawals. Never reuse a key that can withdraw.
# Testnet keys come from testnet.binance.vision (Binance) or
# testnet.bybit.com (Bybit) and are separate from live keys.

BINANCE_API_KEY=
BINANCE_API_SECRET=

# BYBIT_API_KEY=
# BYBIT_API_SECRET=
